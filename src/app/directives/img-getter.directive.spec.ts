import { ImgGetterDirective } from './img-getter.directive';

describe('ImgGetterDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgGetterDirective();
    expect(directive).toBeTruthy();
  });
});
