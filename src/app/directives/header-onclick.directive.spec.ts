import { HeaderOnclickDirective } from './header-onclick.directive';

describe('HeaderOnclickDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderOnclickDirective();
    expect(directive).toBeTruthy();
  });
});
